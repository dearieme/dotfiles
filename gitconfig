[user]
  email = scott@dearie.me.uk
  name = dearieme
[color]
  ui = auto
[alias]
  st = status
  ci = commit
  br = branch
  co = checkout
  df = diff
  dc = diff --cached
  lg = log -p
  lol = log --graph --decorate --pretty=oneline --abbrev-commit --no-merges
  lola = log --graph --decorate --pretty=oneline --abbrev-commit --no-merges --all
  ls = ls-files
  ign = ls-files -o -i --exclude-standard
[diff]
  tool = vimdiff
[difftool]
  prompt = false
[log]
  date = iso8601
[rebase]
  autosquash = true
